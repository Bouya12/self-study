// AtCoder Beginner Contest 140
// C - Maximam Value
#include <bits/stdc++.h>
using namespace std;

int main() {
  int N;
  cin >> N;

  vector<int> B(N - 1);
  for (int i = 0; i < N; i++) {
    cin >> B.at(i);
  }

  vector<int> A(N);
  for (int i = 0; i < N; i++) {
    
  }

  int ans;
  cout << ans << endl;
}