#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (int)(n); i++)
using namespace std;

int main() {
  string S;
  getline(cin, S);

  vector<char> st;
  // stackに格納

  // 計算、出力

  return 0;
}